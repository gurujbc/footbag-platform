{{! Partial: event-card
    Context: a PublicEventSummary object.
    Used on: home (featured events), events/index (upcoming events). }}

<div class="card">
  <div class="card-title">
    <a href="/events/{{eventKey}}">{{title}}</a>
  </div>
  <div class="card-meta">
    <span>{{formatDate startDate}}{{#if endDate}} – {{formatDate endDate}}{{/if}}</span>
    <span>{{formatLocation city region country}}</span>
    {{#if hostClub}}<span>{{hostClub}}</span>{{/if}}
    {{#if registrationStatus}}<span>Registration: {{registrationStatus}}</span>{{/if}}
  </div>
  <span class="badge badge-{{status}}">{{status}}</span>
  {{#if description}}
    <p class="card-description">{{description}}</p>
  {{/if}}
</div>
